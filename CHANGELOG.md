# Changelog

## 0.1.7

**修复：拖动工作区（上下移动排序）时卡死**

- 根因是**视觉顺序与 DOM 顺序不一致**：分组重排用的是 CSS `order` —— 只改**视觉**顺序，DOM 顺序没变；而 DSH 拖动排序时按元素的**真实位置 / DOM 顺序**计算「该插到谁前面」。两者一打架就互相触发：DSH 反复重算并调整 → 我们的 `MutationObserver` 又跟着重算 → 页面卡死，只能刷新。
- 这也解释了为什么之前的修复（去抖 + 幂等写入）没能解决它 —— 那些优化的是"每次重算多贵"，而这里的问题是**两个模块在互相改变对方所依赖的状态**，是逻辑冲突，不是性能问题。
- 现在：**按住工作区行期间（`pointerdown` → 松开）插件完全放手**，一点 DOM 都不动，把控制权完整交给 DSH；松开后（`pointerup` / `pointercancel` / `dragend` / `drop` / 窗口失焦）再重新应用分组。
- 代价仅是"按住工作区的那一瞬间分组暂不重排"，松手立即恢复。

## 0.1.6

**修复：不再改造其他插件插入的菜单项（怀疑是正式环境卡顿的来源）**

- 「会话分组」入口原先会**优先把 folder-tree-sh 插入的「新建文件夹」就地改造成「会话分组」**（改文字、换图标）。那一项属于它自己的状态，被动过之后它后续渲染可能出错甚至反复重试。
- 现在改为**永远自己克隆一项插进菜单**，只动我们自己的节点；folder-tree-sh 的那一项保持原样（它的点击仍被捕获阶段拦截并引导，避免走到它原本会崩的路径）。
- 这也能解释「同一个插件在干净环境正常、在装了一堆插件的正式环境里卡顿」：干净环境没有那一项，代码根本不会走到那条分支。

**修复：菜单项插入可能形成的死循环**

- 原保护条件是「已标记**且我的项还在**」。一旦 React 把我插的项删掉，条件就不再成立 → 每轮 pass 重新插入 → 插入又触发 childList → observer 再跑 → 无限循环。
- 现在用 `WeakSet` 记住处理过的菜单容器，**插过就不再碰**（即使它被删除）。

**本地复现结果（重要）**

在干净环境（sandbox）连续点击 6 个工作区，每次实际开销 **3–8ms**，节点数无任何累积 —— **本地无法复现卡顿**。因此上述两点是基于代码审查得出的最可能原因，需要你在正式环境（3080，装有一批插件）验证。

## 0.1.5

**改进：未归组的会话排到最上方**

- 原先未归组的会话排在所有分组**之后**，现在排到**最上方**，各分组依次排在其后。

**修复：「新会话」不再被自动归组**

- 会话是按**标题**记账的，而 DSH 新建会话的标题是占位文本「新会话」—— 于是**每个新建的会话都会自动继承**上一次分给「新会话」这个标题的那个组。
- 现在 `新会话 / 新对话 / 未命名 / New session / New chat / Untitled` 这类占位标题**一律按未归组处理**，不参与分组记账（改过名字之后照常可以归组）。
- 实测排布：`新会话（未归组、无标头）→ 组甲 → 组乙 → 组丙`。

## 0.1.4

**性能：把封面模块也改成幂等写入（0.1.3 的漏网之鱼）**

- 0.1.3 只把**分组**模块改成幂等写入，**封面**模块仍在每次重算时无条件 `classList.add` / `style.setProperty` / 改 `height` —— 15 个工作区行乘起来就是每轮几十次**真实**样式写入（会触发样式重算）。
- 现在封面也走 `setStyleIf` / `delStyleIf` / `setClassIf`。

**量化对比**（同样触发 30 次无关 DOM 变更，统计样式与类名写入的调用总量）：

| 版本 | setProperty | removeProperty | classList | setAttribute | 总计 |
|---|---|---|---|---|---|
| 0.1.2 原始 | 1095 | 426 | 1281 | 366 | **3168** |
| 0.1.3 只修分组 | 420 | 60 | 240 | 0 | **720** |
| 0.1.4 分组 + 封面 | 180 | 60 | 0 | 0 | **240** |

从 0.1.2 的 3168 次降到 240 次，**降幅 92%**；剩余计数里还包含复现环境里另一个旧实例的开销，插件自身在稳态下已基本不产生写入。

## 0.1.3

**修复：点击会话时页面卡住，必须刷新才能恢复**

- 根因是 0.1.1 / 0.1.2 引入的重排逻辑**吃性能**，两处叠加：
  1. `pass()` 被**直接**当作 `MutationObserver` 回调 —— React 一次点击会产生成百上千次 DOM 变更，于是逐个**同步**跑全量重算（15 个工作区 × 每个都做全树 `querySelectorAll`）。
  2. `applyGrouping()` 每次都**先把所有 `order` / class 清掉、再重新设置** —— 每行两次样式写入，持续触发样式重算与强制布局（layout thrashing）。
- 现在：观察器回调改为 **80ms 去抖**（一次交互只跑一次），并把所有 DOM 写入改成**幂等**（`setStyleIf` / `setClassIf` / `delStyleIf` —— 值没变就不写）。稳态下**一次 DOM 写入都不做**，因此不再触发重排。
- 功能等价性已复验：`groupSection` 直接子元素的 order 排布仍为 `-1 [工作区行] → 0..N [会话] → 9999 [附属按钮]`。

## 0.1.2

**修复：某个工作区的会话看起来「跑进」了下一个工作区**（0.1.1 引入的回归）

- 0.1.1 的重排逻辑把「列表容器的直接子元素」里非会话、非工作区行的元素统一设成 `order: 9999`。但实测 `groupSection` 的**直接子元素只有一个容器**，工作区行是在那个容器**里面** —— 于是 `c === wsRow` 这个判断永远不成立，工作区行被当成"附属元素"推到了整个列表**最后**，看起来就是「A 的会话落进了 B 的列表里」。
- 现在改用 `c.querySelector(ROW_SEL)` 判断工作区行容器，给它 `order: -1`（永远最前）；会话从 0 起排；「展开其余 N 个会话」这类附属按钮才是 9999。
- 实测排布：`#0 order=-1 [工作区行]` → `#1..6 [会话]` → `#7 order=9999 [展开其余]`。

## 0.1.1

**修复：会话分组在会话较多时「失效」**

- 此前只能**按会话的原生顺序**插分组标头。同组会话一旦交错，标头只出现在第一个成员处，后面那些成员在视觉上被并进上一个标头 —— 会话一多就明显错乱（例如「市场插件」名下压了 11 个其实分属三组的会话）。
- 现在改为 **flex + order 的纯样式重排**：把列表容器 `groupSection` 设为 `flex-column`，给每个会话行的 **wrapper** 设 `order`（实测每个会话行各有一个 `SPAN._root_…` 包裹，所以 `order` 必须设在 wrapper 上、flex 必须设在 `groupSection` 上）。同组会话真正聚到一起，**全程只写内联样式，一个节点都不移动**。
- 副作用：DSH 自带的会话拖拽排序仍按 DOM 顺序记录，视觉顺序与拖拽手感可能不完全一致。

**修复：干净环境里没有分组入口**

- 入口此前寄生在 `folder-tree-sh` 插入的「新建文件夹」菜单项上；在没装该插件的干净 profile 里完全不存在。现在改为「优先就地改造它，不存在就自己 clone 一项插进菜单」（克隆节点不属于 React 树，安全）。

**改进**

- 分组管理面板改为**屏幕垂直居中**并加 0.18s 淡入 —— 原先固定在左下角，点完菜单后容易以为「没反应」。

## 0.1.0 — 首个公开版本

把原先三个各自独立的插件合并成一个（`dsh-sidebar-fold` + `dsh-workspace-cover` + `dsh-sidebar-groups`）。

**新增 / 合并**

- 设置里新增一栏 **「工作区」**，含三个即时生效的开关（`工作区背景` / `工作区名展开收缩` / `工作区下的会话分组`）
- **内置 8 张默认图集**（生活 / 学习 / 论文·学术 / 科技知识 / 健康知识 / 休闲娱乐 / 视频·AI 创作 / 工程），由 `gpt-image-2` 按统一 prompt 骨架生成并做统一后处理；新的 host 路由 `/dsh-cover-preset?id=`
- 封面编辑器：调参时**实时**把画布铺到侧栏那一行；编辑器**就地展开**在对应工作区行下方；「封面行高度」改为全局统一参数
- 会话分组入口改到**工作区行的「⋯」菜单**里（就地改造并接管了原先那个会崩的第三方按钮），点开即锁定该工作区，不再需要手动选
- 封面编辑器不再使用左侧色条（会话行保持原生外观）

**修复**

- 合并版首版给 `MutationObserver` 加了 `attributeFilter:["aria-expanded"]`，而折叠功能自己就会写这个属性 → 属性变化触发观察器 → 再写一次 → **页面卡死**。现改为只观察 `childList`，且所有 `setAttribute` 先比较再写。
- 封面编辑器此前在已保存的成品图上二次编辑会叠加压暗；现在另存一份原图（`<key>-src.png`）供二次微调。

**已知问题**

- 设置面板 UI 与三块功能的完整交互回归尚未在真实环境跑完（详见 README「已知限制」）。
- 开发目标为 `dsh 0.1.5-rc.2`，DSH 升级后若 DOM 层级或 slot 名变化需要适配。
